// App principal que exibe a interface mobile do conversor
import InterfaceConversor from './InterfaceConversor';

function App() {
  // Apenas renderiza a interface do conversor
  return <InterfaceConversor />;
}

export default App;
